server:
  port: 8080
  servlet:
    context-path: /api/product-backend
spring:
  application:
    name: demo
  datasource:
    url: jdbc:postgresql://${DB_HOST:db}:${DB_PORT:5432}/${DB_NAME:product_db}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  sql:
    init:
      mode: never
  jpa:
    hibernate:
      ddl-auto: update

jwt:
  secret: ${JWT_SECRET:8w9eK7sVh3FQ2mJ6lP0rXyZtN1bQ4uDkFvHsT2cW+Eo=}

grpc:
  client:
    log-ingestor:
      address: ${LOGS_SERVER_HOST}
